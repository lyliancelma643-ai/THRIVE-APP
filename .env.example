# ─────────────────────────────────────────
# THRIVE APP — Variables d'environnement
# Copie ce fichier en .env.development, .env.staging, .env.production
# Ne JAMAIS commiter les fichiers .env réels
# ─────────────────────────────────────────

# App
NODE_ENV=development
APP_URL=http://localhost:3000

# Database (Supabase)
# SUPABASE_URL / SERVICE_ROLE : côté serveur (edge functions). Ne jamais exposer
# la service_role au client — le web/mobile utilisent les NEXT_PUBLIC_/EXPO_PUBLIC_.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# RevenueCat (Mobile IAP)
REVENUECAT_API_KEY_IOS=
REVENUECAT_API_KEY_ANDROID=

# Stripe (paiement des forfaits, web) — secrets des edge functions Supabase :
#   supabase secrets set STRIPE_SECRET_KEY=... STRIPE_WEBHOOK_SECRET=...
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Sentry (monitoring erreurs — INERTE tant que le DSN est vide)
# Web : poser NEXT_PUBLIC_SENTRY_DSN dans Vercel (Production + Preview).
# Edge functions : supabase secrets set SENTRY_DSN=<même DSN ou projet dédié>
# Upload des source maps (optionnel) : SENTRY_AUTH_TOKEN + SENTRY_ORG + SENTRY_PROJECT
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Web Push / PWA (VAPID) — générer une paire : node -e "const c=require('crypto').createECDH('prime256v1');c.generateKeys();console.log('pub',c.getPublicKey().toString('base64url'));console.log('priv',c.getPrivateKey().toString('base64url'))"
# Publique → Vercel (NEXT_PUBLIC_VAPID_PUBLIC_KEY) ; paire complète → secrets Supabase pour send-web-push
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:contact@thrive.app

# Web (clés PUBLIQUES — embarquées dans le bundle, protégées par la RLS)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Mobile (Expo — variables embarquées dans le bundle : clés PUBLIQUES uniquement)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
EXPO_PUBLIC_REVENUECAT_API_KEY_IOS=
EXPO_PUBLIC_REVENUECAT_API_KEY_ANDROID=
